

pipeline {
    agent any
    stages {
        stage("Copy files to ansible_server") {
            steps {
                script {
                    echo "Copying all necessary file to ansible server"
                    sshagent(['SSH_Key']) {
    // some block
    
                                            }
                }
            }
        }
        
    }   
}